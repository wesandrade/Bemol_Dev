@isTest
public class ConfirmTranscricaoControllerTest {
    @isTest static void test(){
        ConfirmTranscricaoController controller = new ConfirmTranscricaoController();
        System.assertEquals(controller.url, null);
    }
}